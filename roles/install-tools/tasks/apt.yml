- name: "Installing apt packages"
  ansible.builtin.apt:
    name:
      - postgresql
      - curl
      - zip
      - unzip
      - exiftool
      - sqlite3
      - pipenv
      - htop
      - gh
      - build-essential
      - gcc
    state: present
  become: true